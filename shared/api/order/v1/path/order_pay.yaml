---
post:
  summary: Создание заказа
  tags:
    - Order
  parameters:
    - name: order_uuid
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../component/order_pay_request.yaml"
  responses:
    "200":
      required: true
      content:
        application/json:
          schema:
            $ref: "../component/order_pay_response.yaml"
    "400":
      description: Ошибка валидации
